// page for a single recipe with everything in it

// should contain a title, source of the recipe (video, website, or self source), ingredients, notes about the recipe

const RecipePage = () => {
  return <div>RecipePage</div>;
};

export default RecipePage;
